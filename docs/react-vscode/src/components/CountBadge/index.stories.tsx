import React from 'react';
import { CountBadge } from '@kbjdev/react-vscode';
import type { Meta } from '@storybook/react';

const meta: Meta = {
  title: 'Components/Badge/CountBadge',
  component: CountBadge,
};

export { default as Default } from './DefaultCountBadge';

export default meta;
