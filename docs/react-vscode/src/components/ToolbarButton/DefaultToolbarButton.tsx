import React, { FC } from 'react';
import { ToolbarButton } from '@kbjdev/react-vscode';

const DefaultToolbarButton: FC = () => <ToolbarButton icon="ellipsis" />;

export default DefaultToolbarButton;
