import React, { FC } from 'react';
import { ToolbarButton } from '@kbjdev/react-vscode';

const DisabledToolbarButton: FC = () => <ToolbarButton icon="arrow-right" disabled />;

export default DisabledToolbarButton;
